import { __experimentalUnitControl as UnitControl } from '@wordpress/components';

const UnitControl = ({ value, onChange }) => {
    return (
        <UnitControl
            value={value}
            onChange={onChange}
        />
    )
}

export default UnitControl;